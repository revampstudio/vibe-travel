{
  "name": "vibe-travel",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix apps/mobile run start",
    "web": "npm --prefix apps/mobile run web",
    "android": "npm --prefix apps/mobile run android",
    "ios": "npm --prefix apps/mobile run ios",
    "doctor": "npm --prefix apps/mobile run doctor",
    "worker:dev": "npm --prefix worker run dev",
    "worker:deploy": "npm --prefix worker run deploy",
    "worker:check": "npm --prefix worker run check",
    "worker:types": "npm --prefix worker run types"
  }
}
